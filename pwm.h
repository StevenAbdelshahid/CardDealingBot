/*-----------------------------------------------------------------*
 *  pwm.h  ? public interface to the PWM module                    *
 *-----------------------------------------------------------------*/
#ifndef PWM_H
#define PWM_H

#include <stdint.h>
#include <stdbool.h>

/*  <<< REMOVE ANY FUNCTION BODY FOR CheckNullEvents HERE >>>      */
uint8_t CheckNullEvents(void);   /* prototype only                 */

/* ---- the rest of your PWM prototypes go below ----------------- */
/*  uint8_t  PWM_Init(void);                                       */
/*  uint8_t  PWM_SetFrequency(uint32_t NewFreq);                   */
/*  ...                                                            */

#endif  /* PWM_H */
